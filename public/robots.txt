User-agent: *
Allow: /

# Query strings carry calculator inputs. They are useful to share but must not
# be indexed as separate pages — every tool page declares a clean canonical.
Disallow: /*?

Sitemap: https://example.com/sitemap.xml
